\documentclass{article}

% ============================================================================
% Packages
% ============================================================================
\usepackage[margin=1in]{geometry}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{algorithm} 
\usepackage{caption} 
\usepackage{tikz}

% WE USE ALGPSEUDOCODEX INSTEAD OF ALGPSEUDOCODE
% 'noEnd=true' hides "end if", "end for" to match the style of your image (Baum et al.)
% 'indLines=true' draws the vertical lines automatically
\usepackage[noEnd=true, indLines=true]{algpseudocodex}

% ============================================================================
% Configuration & Custom Commands
% ============================================================================

% 1. Customize the Vertical Lines (make them black and thin like the image)
\tikzset{algpxIndentLine/.style={draw=black, very thin}}

% 2. Custom Inputs/Outputs with Line Jump
% Input commands are not defined, so use \algnewcommand
\algnewcommand\algorithmicinput{\textbf{Input:}}
\algnewcommand\Input{\item[\algorithmicinput]\mbox{}\par} 
% Output commands are already defined, so use \algrenewcommand
\algrenewcommand\algorithmicoutput{\textbf{Output:}}
\algrenewcommand\Output{\item[\algorithmicoutput]\mbox{}\par}

% 3. Keywords - ensure proper spacing
\algnewcommand\And{\ \textbf{and}\ }
\algnewcommand\Or{\ \textbf{or}\ }

% 4. Comments styled with //
% We redefine the comment symbol to be //
\algrenewcommand\algorithmiccomment[1]{\hfill // #1}
\newcommand{\CommentLine}[1]{
    \State // \textit{#1}
}

% ============================================================================
% The Document
% ============================================================================
\begin{document}
\pagestyle{empty}
\allowdisplaybreaks[1] 
\setcounter{page}{1}

% ============================================================================
% Algorithm 6
% ============================================================================

\setcounter{algorithm}{5}
\begin{center}
    % Top Rule
    \hrule height 0.8pt
    \vspace{0.3em}
    % Caption
    \captionof{algorithm}{Label-setting algorithm with time-budgeted UB pruning, scoring, and early stopping}
    \label{alg:label_setting_time_budgeted_ub_scoring_early_stopping}
    \vspace{0.2em}
    % Middle Rule
    \hrule height 0.5pt
\end{center}

In this initial version, we use the following scoring formula: 

$Score(L) = |C| + (1 - \tau)UB$; where $\tau = \frac{t - \text{start\_time}}{1440}$
\vspace{0.5em}

\begin{algorithmic}[1] % [1] enables line numbering
    
    % INPUT
    \Input
    \begin{itemize}
        \item $V$. Set of nodes
        \item $E_s$. Set of scheduled edges $(v,u,dep,arr)$
        \item $E_t$. Set of transfer edges $(v,w,\Delta)$
        \item $\text{country}(v)$. Country associated with node $v$
        \item $R(v,b)$. Precomputed reachable country sets
        \item $MTT(c)$. Precomputed minimum transit times for each country
        \item $\text{bucket}(t)$. Time bucket mapping function
        \item $\text{record}$. Current record to beat (e.g. 13)
    \end{itemize}

    % OUTPUT
    \Output
    \begin{itemize}
        \item $P$. Sequence of labels representing a route within 24 hours
    \end{itemize}
    \vspace{0.5em} 

    % INITIALIZATION
    \State Initialize $\text{Labels}(v) \leftarrow \emptyset$ for all $v \in V$
    \State Initialize priority queue $Q \leftarrow \emptyset$ \Comment{ordered by decreasing score}
    \State Initialize $\text{best\_label} \leftarrow \text{null}$

    \vspace{0.5em}
    \CommentLine{Initialize labels at departures}
    \ForAll{$v \in V$}
        \ForAll{scheduled edges $(v,u,dep,arr) \in E_s$}
            \State $L \leftarrow (u,arr,\{\text{country}(u)\},\text{pred}=\text{null},\text{start\_time}=dep)$
            \State $\text{Labels}(u) \leftarrow \text{Labels}(u) \cup \{L\}$
            \State compute $\text{score}(L)$
            \State $Q \leftarrow Q \cup \{L\}$
        \EndFor
    \EndFor

    \vspace{0.5em}
    \CommentLine{Main loop}
    \While{$Q \neq \emptyset$}
        \State extract $L=(v,t,C,\text{pred},\text{start\_time})$ with maximum score from $Q$

        \CommentLine{Enforce 24-hour journey limit (1440 absolute minutes)}
        \If{$t - \text{start\_time} > 1440$}
            \State \textbf{continue}
        \EndIf

        \CommentLine{Upper bound computation (Time-Budgeted)}
        \State $b \leftarrow \text{bucket}(t)$
        \State $U \leftarrow R(v,b) \setminus C$
        \State Sort $U$ as a sequence $(c_1, c_2, \ldots, c_k)$ such that $MTT(c_1) \le MTT(c_2) \le \ldots \le MTT(c_k)$
        \State $\text{time\_spent} \leftarrow 0$
        \State $\text{UB} \leftarrow 0$
        \State $\text{remaining\_time} \leftarrow 1440 - (t - \text{start\_time})$
        \For{$i = 1$ to $|U|$}
            \State $\text{time\_spent} \leftarrow \text{time\_spent} + MTT(c_i)$
            \If{$\text{time\_spent} \le \text{remaining\_time}$}
                \State $\text{UB} \leftarrow \text{UB} + 1$
            \Else
                \State \textbf{break}
            \EndIf
        \EndFor

        \CommentLine{Record-based pruning}
        \If{$|C| + \text{UB} \le \text{record}$}
            \State \textbf{continue}
        \EndIf

        \CommentLine{Update best label}
        \If{$\text{best\_label}=\text{null} \lor |C|>|\text{best\_label}.C|$}
            \State $\text{best\_label} \leftarrow L$
            \If{$|C| > \text{record}$}
                \State \textbf{break} \Comment{early stopping: record beaten}
            \EndIf
        \EndIf

        \vspace{0.5em}
        \CommentLine{Extend via scheduled edges}
        \ForAll{$(v,u,dep,arr)\in E_s$}
            \If{$dep \ge t \land dep - \text{start\_time} \le 1440$}
                \State $t_{\text{new}} \leftarrow arr$
                \If{$t_{\text{new}} - \text{start\_time} > 1440$}
                    \State \textbf{continue}
                \EndIf
                \State $C' \leftarrow C \cup \{\text{country}(u)\}$
                \State $L' \leftarrow (u,t_{\text{new}},C',\text{pred}=L,\text{start\_time}=\text{start\_time})$
                
                \Statex % Empty line for better readability of the multiline condition
                \If{$\nexists L''\in\text{Labels}(u)$ such that \par
                    \hskip\algorithmicindent $(L''.t \le t_{\text{new}} \land L''.C \supseteq C' \land 
                    (L''.t < t_{\text{new}} \lor L''.C \neq C'))$}
                    
                    \State remove all $L''\in\text{Labels}(u)$ such that \par
                    \hskip\algorithmicindent $(t_{\text{new}} \le L''.t \land C' \supseteq L''.C \land 
                    (t_{\text{new}} < L''.t \lor C' \neq L''.C))$
                    \State $\text{Labels}(u) \leftarrow \text{Labels}(u) \cup \{L'\}$
                    \State compute $\text{score}(L')$
                    \State $Q \leftarrow Q \cup \{L'\}$
                \EndIf
            \EndIf
        \EndFor

        \vspace{0.5em}
        \CommentLine{Extend via transfers}
        \ForAll{$(v,w,\Delta)\in E_t$}
            \State $t_{\text{new}} \leftarrow t + \Delta$
            \If{$t_{\text{new}} - \text{start\_time} > 1440$}
                \State \textbf{continue}
            \EndIf
            \State $L' \leftarrow (w,t_{\text{new}},C,\text{pred}=L,\text{start\_time}=\text{start\_time})$
            
            \Statex % Empty line
            \If{$\nexists L''\in\text{Labels}(w)$ such that \par
                \hskip\algorithmicindent $(L''.t \le t_{\text{new}} \land L''.C \supseteq C \land 
                (L''.t < t_{\text{new}} \lor L''.C \neq C))$}
                
                \State remove all $L''\in\text{Labels}(w)$ such that \par
                \hskip\algorithmicindent $(t_{\text{new}} \le L''.t \land C \supseteq L''.C \land 
                (t_{\text{new}} < L''.t \lor C \neq L''.C))$
                \State $\text{Labels}(w) \leftarrow \text{Labels}(w) \cup \{L'\}$
                \State compute $\text{score}(L')$
                \State $Q \leftarrow Q \cup \{L'\}$
            \EndIf
        \EndFor
    \EndWhile

    \vspace{0.5em}
    \CommentLine{Reconstruct path}
    \State $P \leftarrow$ empty sequence
    \State $L \leftarrow \text{best\_label}$
    \While{$L \neq \text{null}$}
        \State prepend $L$ to $P$
        \State $L \leftarrow L.\text{pred}$
    \EndWhile
    \State \Return $P$

\end{algorithmic}

% Bottom Rule
\hrule height 0.8pt
\end{document}
